import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles((theme) => ({
  main: {
    position: 'relative',
  },
  mobile: {
    paddingLeft: '6px',
    paddingRight: '6px',
  },
}));